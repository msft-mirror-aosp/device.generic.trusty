service vendor.boot-default /vendor/bin/hw/android.hardware.boot-service.mock-trusty
    class early_hal
    user root
    group root
    seclabel u:r:hal_bootctl_default:s0
    interface aidl android.hardware.boot.IBootControl/default

on post-fs
    mkdir /metadata/boot_control_mock 0770 root root
